name: Build and test containers

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

env:
  DOTNET_VERSION: "10.0.401"
  MYSQL_ROOT_PASSWORD: BaKHQ5zr

jobs:
  unit-tests:
    name: xUnit tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Restore
        run: dotnet restore Storefront.sln

      - name: Build
        run: dotnet build Storefront.sln -c Release --no-restore

      - name: Test frontend and backend
        run: dotnet test Pages/AddItem.cshtml.cs -c Release --no-build --verbosity normal
        run: dotnet test Pages/Error.cshtml.cs -c Release --no-build --verbosity normal
        run: dotnet test Pages/Index.cshtml.cs -c Release --no-build --verbosity normal
        run: dotnet test Pages/Privacy.cshtml.cs -c Release --no-build --verbosity normal

  container-tests:
    name: Build container test stages
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Run frontend and backend tests in image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile
          target: test
          push: false
          load: false

  compose-smoke:
    name: Compose build and smoke
    runs-on: ubuntu-latest
    needs: [unit-tests, container-tests]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4

      - name: Build and start stack
        run: docker compose up --build -d --wait

      - name: Check backend
        run: curl --fail --retry 10 --retry-delay 3 http://localhost:5080/health || curl --fail --retry 10 --retry-delay 3 http://localhost:5080/

      - name: Check frontend
        run: curl --fail --retry 10 --retry-delay 3 http://localhost:5081/

      - name: Compose logs on failure
        if: failure()
        run: docker compose logs

      - name: Tear down
        if: always()
        run: docker compose down -v
